\documentclass[11pt,a4paper]{article}

\usepackage{graphicx}
\usepackage{multirow}

\usepackage{tikz}
\usetikzlibrary{shapes.geometric, shapes.misc, arrows.meta, positioning, fit, backgrounds, calc}

% Diagram
\usepackage{booktabs}
\usepackage{caption}
\captionsetup[table]{skip=10pt}

\usepackage[T1]{fontenc}
\usepackage{listings}

% Definitions
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}

\newtheorem{definition}{Definition}

\usepackage{xcolor}

\usepackage{hyperref}
\usepackage[normalem]{ulem}

\lstdefinelanguage{ASP}{
  sensitive=true,
  morestring=[b]",
  morekeywords={not},
}

\lstset{
  basicstyle=\ttfamily\small,
  keywordstyle=\color{blue}\bfseries,
  keywordstyle=[2]\color{purple}\bfseries,
  commentstyle=\color{gray}\itshape,
  stringstyle=\color{teal},
  breaklines=true,
  columns=fullflexible,
  keepspaces=true,
  frame=single,
  backgroundcolor=\color{gray!8},
  rulecolor=\color{black!20},
}

\begin{document}

\title{LAMP: ASP Rule Induction and Optimization with LLMs}

\author{David Bunker \\ University of Potsdam}

\date{}
\maketitle

\thispagestyle{plain}
\pagestyle{plain}

\begin{abstract}
Answer Set Programming (ASP) offers a compelling mechanism for knowledge representation and reasoning, however developing such programs remains challenging. This work introduces the LLM to ASP Modeling Protocol (LAMP), a system by which an LLM iteratively generates and refines candidate ASP rules with each candidate verified by the Clingo solver. LAMP is evaluated against the leading Inductive Logic Programming (ILP) system Inductive Learning of Answer Set Programs (ILASP), across 120 synthetic benchmarks of varying complexity. ILASP achieves 97.5\% accuracy, with failures due to search space timeouts. Among LLMs, \textit{gpt-5-mini} reaches 100\% accuracy, \textit{gpt-oss:20b} 95.8\%, and \textit{qwen3-coder:30b} 45.0\%. Although ILASP was on average faster, \textit{gpt-5-mini} solved all benchmarks on which ILASP timed out. Both LAMP and ILASP are also able to reduce literal counts and increase stratification to a similar degree. Finally, feature analysis is used to determine predictors of LLM success or failure.
\end{abstract}

\section{Introduction}

Knowledge representation and reasoning problems arise across many domains, including software and hardware customization, industrial manufacturing, service composition and others. These problems are characterized by combinatorial constraints that determine which combinations of components are valid. Answer Set Programming (ASP) models such problems, offering clear semantics with expressive capabilities for constraints, defaults, and combinatorial reasoning.

However, writing ASP programs that are both correct and performant remains challenging, as modeling choices can significantly affect solver behavior. Recent progress in Large Language Models (LLMs) offers new opportunities to reduce this modeling burden. LLMs have shown strong capabilities in code generation and program synthesis, suggesting they may assist in deriving ASP encodings from high level natural language specifications \cite{ishay2023leveraginglargelanguagemodels}.

The LLM to ASP Modeling Protocol (LAMP) is designed to guide LLMs in deducing and optimizing ASP rules from given instance facts and expected answer sets. LAMP provides a structured interaction protocol that enables refinement of ASP encodings, with the goal of improving both correctness and performance. Beyond proposing the system itself, LAMP can be used as a framework for evaluating the capabilities of different LLMs in ASP program generation.

Both LAMP and the leading Inductive Logic Programming (ILP) system Inductive Learning of Answer Set Programs (ILASP) \cite{law2020ilaspinductivelearninganswer} address the same underlying problem class. Given a set of instance facts and an expected answer set, find a program within a bounded hypothesis space that produces the correct answer set. ILASP solves this via exhaustive search under the Learning from Answer Sets (LAS) framework while LAMP replaces exhaustive search with LLM guided generation and Clingo based iterative verification. As the expected input and output are the same, a controlled comparison can be made between symbolic ILP search and LLM based generation on the same class of tasks.

The relevant research questions of this work are the following.

\noindent
\textbf{RQ1.} Is a structured approach for LLM assisted ASP modeling possible using the LAMP system, and under what conditions does it produce correct ASP encodings?

\noindent
\textbf{RQ2.} How do LLMs and ILASP compare in terms of accuracy, speed, and program conciseness on ASP rule induction tasks?

\noindent
\textbf{RQ3.} Which features of the ASP benchmark most strongly predict LLM success or failure in LAMP?

This paper investigates the current capabilities of Large Language Models (LLMs) regarding Answer Set Programming (ASP) and offers directions for future research at the intersection of artificial intelligence and constraint solving. The LAMP system can be compared against similar systems such as those based on LLMs fine tuned to ASP \cite{coppolillo2024llaspfinetuninglargelanguage}, iterative scheduling and plan generation with LLMs and ASP \cite{lin2024clmaspcouplinglargelanguage}, and LLM based systems that can learn directly from ASP answer sets \cite{borroto2025questionansweringllmslearning}, as well as solver in the loop frameworks that use Clingo feedback to iteratively improve LLM generated ASP \cite{schrader2025solverintheloopframeworkimprovingllms}.

\section{Background}

This section presents the core background underpinning LAMP including ASP, ILASP, and LLMs.

\subsection{Answer Set Programming}

Answer Set Programming (ASP) is a declarative programming paradigm based on stable model semantics \cite{DBLP:series/synthesis/2012Gebser}, well suited to combinatorial search and knowledge intensive reasoning tasks. An ASP program consists of rules over atoms and literals, and its solutions are the stable models, referred to as \textit{answer sets}, of that program. The following paragraphs introduce the syntactic primitives, rule structure, grounding procedure, and stable model semantics used throughout this paper.

\paragraph{Syntactic Primitives.}
A \textit{term} is either a constant represented as a lowercase symbol or integer such as\ $a$, $0$ or a variable represented as an uppercase symbol, such as\ $X$. A \textit{predicate} $p/k$ is a symbol of arity $k \geq 0$. Applying it to $k$ terms yields an \textit{atom} $p(t_1,\ldots,t_k)$. A ground atom contains no variables. A \textit{literal} is either a positive literal $a$, an atom, or a \textit{default negated} literal $\mathit{not}\; a$, where $\mathit{not}$ denotes negation as failure and $\mathit{not}\; a$ holds whenever $a$ cannot be derived.

\paragraph{Rules and Programs.}
An ASP program $\Pi$ is a finite set of \textit{rules} of the following form.

\begin{equation}
    h \leftarrow b_1, \ldots, b_m, \mathit{not}\; c_1, \ldots, \mathit{not}\; c_n
\end{equation}

\noindent Here $h$ is the \textit{head}, a classical atom or empty, each $b_i$ is a positive body literal, and $\mathit{not}\; c_j$ denotes default negation of atom $c_j$. When $m = n = 0$ the rule is a \textit{fact} and when $h$ is empty the rule is a \textit{constraint} written $\leftarrow \text{body}$, eliminating any answer set in which the body is satisfied. A body is satisfied when every positive literal $b_i$ is in the answer set and every negated atom $c_j$ is absent from it, which in a normal rule would force $h$ into the answer set, but with no head there is nothing to derive, so the answer set is eliminated instead.

\paragraph{Variables and Grounding.}
Rules are written with first order variables---symbols such as $X$ and $Y$ that range over concrete objects rather than truth values---allowing compact expression over large domains. Prior to solving, a \textit{grounder}, such as \textsc{Gringo}, substitutes all relevant terms for each variable, producing an equivalent propositional ground program $\Pi_g$. A rule is \textit{safe} if every variable occurring in it also appears in a positive body literal, which ensures that grounding is well defined.

\paragraph{Semantics.}
Let $\mathcal{A}$ denote the Herbrand base set of all ground atoms. Given a ground program $\Pi$ and a set $S \subseteq \mathcal{A}$, the \textit{Gelfond--Lifschitz reduct} $\Pi^S$ is obtained by the steps.
\begin{enumerate}
    \item Remove every rule whose body contains $\mathit{not}\; c$ for some $c \in S$
    \item Remove all negative literals from the bodies of remaining rules.
\end{enumerate}
$S$ is a \textit{stable model}, referred to in ASP as an \textit{answer set}, of $\Pi$ if and only if $S$ is the \textit{minimal model} of the reduct $\Pi^S$. Because $\Pi^S$ is negation free, it has a unique minimal model, the smallest set of atoms satisfying every rule, where no atom is included unless it is forced by some rule in $\Pi^S$. A program may have zero, one, or multiple answer sets and solutions to the encoded problem correspond exactly to its answer sets. $AS(\Pi)$ is used to denote the set of answer sets of a program $\Pi$.

Modern ASP solvers, such as Clingo \cite{gebser2014clingoasp}, extend the core language with aggregates, choice rules, disjunctive heads, and optimization statements. LAMP uses Clingo as its verification oracle but operates over a restricted subset of normal rules only, as defined in Section~\ref{sec:benchmark}.

\paragraph{Solving Complexity.}
Deciding whether a ground ASP program has an answer set is $\Sigma_2^P$-complete \cite{bichler2016powernongroundrulesanswer}. This places ASP one level above $\mathsf{NP}$ in the polynomial hierarchy, reflecting the two sources of computational hardness, the nondeterministic search over candidate sets $S \subseteq \mathcal{A}$, and the co-$\mathsf{NP}$ check that no proper subset of $S$ is also a stable model of $\Pi^S$. This work restricts to the \textit{normal} subset, where the bound drops one level to $\mathsf{NP}$-complete \cite{bichler2016powernongroundrulesanswer}. LAMP would apply unchanged to the disjunctive case.

\subsection{Learning from Answer Sets}\label{sec:ilasp}

Inductive Learning of Answer Set Programs (ILASP) \cite{law2020ilaspinductivelearninganswer} is a framework for Inductive Logic Programming (ILP) that learns ASP programs from examples, defining the same task that LAMP approaches via LLM guided generation. A learning task is specified by a \textit{hypothesis space} $\mathcal{H}$ constraining which rules may be learned, a \textit{background knowledge} program $B$ consisting of ASP rules that are given rather than learned, and a set of \textit{learning examples} $E$ specifying constraints on the answer sets that the learned hypothesis must produce. The following paragraphs define each component and describe the conflict driven search procedure ILASP uses to find an optimal hypothesis.

\paragraph{Hypothesis Space.}
The hypothesis space $\mathcal{H}$ is defined by a \textit{mode bias}, which constrains the structure of rules that may appear in a learned hypothesis. A \textit{mode declaration} is either a \textit{head declaration} $\mathit{modeh}(a)$ or a \textit{body declaration} $\mathit{modeb}(a)$, specifying which atoms may appear in rule heads and bodies respectively. Each atom in a mode declaration may contain \textit{placeholder terms} of the form $\mathit{\#var}(t)$ (a variable of type $t$) or $\mathit{\#con}(t)$ (a constant of type $t$), which are instantiated during the search. A \textit{hypothesis} $H \in \mathcal{H}$ is a set of ASP rules consistent with the mode bias.

\paragraph{Learning Examples.}
Examples place constraints on the answer sets of $B \cup H$.
\begin{itemize}
    \item \textbf{Positive examples} $e^+ \in E^+$, a set of ground atoms. At least one answer set of $B \cup H$ must be a superset of $e^+$.
    \item \textbf{Negative examples} $e^- \in E^-$, a set of ground atoms. No answer set of $B \cup H$ may be a superset of $e^-$.
\end{itemize}

\noindent To illustrate, suppose the goal is to learn which individuals are \texttt{allowed} entry, given background facts about adults and minors.

\begin{lstlisting}[language=ASP, caption={Simple ILASP learning task.}]
% Background knowledge B
adult(alice). adult(carol). minor(bob).

% Hypothesis space
#modeh(allowed(var(person))).
#modeb(adult(var(person))).
#modeb(minor(var(person))).

% alice must be allowed
#pos(e1, { allowed(alice) }, { }).

% bob must NOT be allowed
#neg(e2, { }, { allowed(bob) }).
\end{lstlisting}

\noindent The positive example forces $H$ to derive \texttt{allowed(alice)}, ruling out an empty hypothesis. The negative example rules out the overly general rule \texttt{allowed(X).}\ Together they uniquely identify \texttt{allowed(X) :- adult(X).} as the intended hypothesis.

\paragraph{Learning Task.}
A \textit{learning task} is a tuple $T = \langle B, \mathcal{H}, E^+, E^- \rangle$. A hypothesis $H \in \mathcal{H}$ is an \textit{inductive solution} of $T$ if it covers all examples, meaning every positive example is satisfied by some answer set of $B \cup H$, and no answer set of $B \cup H$ satisfies any negative example. When hypotheses are assigned a \textit{cost} (typically the sum of rule lengths), the learning task seeks an \textit{optimal} solution. This is the inductive solution of minimum cost.

\paragraph{Search Procedure.}
ILASP reduces the learning task to a sequence of ASP solving problems. The hypothesis space is encoded as an ASP metaprogram, and the search for a covering hypothesis is directed by a \textit{conflict driven} procedure \cite{law2020ilaspinductivelearninganswer}. Candidate hypotheses are proposed, checked against all examples, and conflicts, examples not yet covered, are used to prune the space and guide subsequent iterations. This conflict driven approach, implemented in the \textsc{FastLAS} system, scales substantially better than exhaustive enumeration of $\mathcal{H}$ \cite{law2022conflictdriveninductivelogicprogramming}.

\paragraph{Solving Complexity.}
Learning an optimal inductive solution under the full ILASP framework is $\Sigma_3^P$-complete \cite{law2020ilaspinductivelearninganswer}, one level above the $\Sigma_2^P$-hardness of ASP itself. The additional level reflects the search over hypotheses $H \in \mathcal{H}$ layered on top of the $\Sigma_2^P$ check that $H$ is a valid inductive solution. Restricting to the normal fragment lowers the verification step by one level, so learning over the LAMP hypothesis space sits at $\Sigma_2^P$. This theoretical hardness motivates an alternative approach. Rather than exhaustive symbolic search, LAMP replaces the hypothesis search with LLM guided generation, bypassing the direct hypothesis space search.

\subsection{Large Language Models for Code Generation}

Large Language Models (LLMs) are neural language models trained on large amounts of text and source code, enabling strong performance on code generation tasks. Given a structured prompt, an LLM generates a candidate program token by token. Its output is non deterministic and may require post processing or validation against a formal oracle.

LLMs have been shown to generate logic programs from natural language specifications \cite{ishay2023leveraginglargelanguagemodels, li2025logicofthoughtempoweringlargelanguage, lin2024clmaspcouplinglargelanguage} and to perform inductive logic programming when paired with a formal inference engine \cite{gandarela2025inductivelearninglogicaltheories}. A key pattern across these systems is \emph{iterative refinement} where the LLM generates a candidate, the candidate is verified symbolically, and any mismatch is encoded as feedback for the next prompt. LAMP instantiates this pattern for ASP, using Clingo as the verification oracle and Clingo derived feedback to guide successive generation attempts.

\section{Methodology}

\begin{figure}[t]
  \centering

  % ── Colour palette ──────────────────────────────────────────────────────
  \colorlet{inputcol}{blue!12}
  \colorlet{llmcol}{green!15}
  \colorlet{clingo}{orange!18}
  \colorlet{evalcol}{violet!12}
  \colorlet{outcol}{red!12}
  \colorlet{loopbg}{black!3}

  \tikzset{
    proc/.style={
      rectangle, rounded corners=5pt,
      minimum width=3.4cm, minimum height=1.0cm,
      text centered, text width=3.2cm,
      font=\sffamily\small,
      draw=black!50, line width=0.6pt,
      fill=#1
    },
    proc/.default=white,
    decision/.style={
      diamond, aspect=2.6,
      minimum width=3.8cm, minimum height=1.1cm,
      text centered, text width=3.0cm,
      font=\sffamily\small,
      draw=black!50, line width=0.6pt,
      fill=clingo
    },
    terminal/.style={
      rectangle, rounded corners=14pt,
      minimum width=3.4cm, minimum height=0.9cm,
      text centered, text width=3.0cm,
      font=\sffamily\small\bfseries,
      draw=black!50, line width=0.6pt,
      fill=#1
    },
    terminal/.default=outcol,
    arr/.style={
      -{Stealth[length=6pt, width=4pt]},
      line width=1.0pt, color=black!60
    },
    darr/.style={
      -{Stealth[length=6pt, width=4pt]},
      line width=0.9pt, color=black!60, dashed
    },
    grplabel/.style={font=\sffamily\tiny\bfseries, text=black!55},
  }

  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[node distance=0.55cm and 1.6cm]

    \node[proc=inputcol, minimum width=3.4cm]
      (facts) {Instance Facts\\[2pt]\texttt{\scriptsize d0(o0). d0(o1). d1(o1).}};
    \node[proc=inputcol, minimum width=3.4cm, below=of facts]
      (model) {Expected Stable Model\\[2pt]\texttt{\scriptsize \{d0(o0), d2(o0),\ldots\}}};
    \node[proc=inputcol, minimum width=3.4cm, below=of model]
      (format) {Hypothesis Space $\Phi$\\[2pt]\footnotesize\textit{arity-1, single var, bounded literals}};

    % \node[grplabel, left=0.15cm of facts, rotate=90, anchor=south] {INPUTS};

    \node[proc=llmcol, minimum width=3.0cm, right=1.8cm of model]
      (prompt) {Construct Prompt $p_i$};
    \node[proc=llmcol, minimum width=3.8cm, minimum height=1.3cm, right=1.5cm of prompt]
      (llm) {\textbf{LLM}};
    \node[proc=llmcol, minimum width=3.0cm, below=0.9cm of llm]
      (parse) {Parse Output to ASP};
    \node[proc=clingo, minimum width=3.0cm, below=0.8cm of parse]
      (clingonode) {\textbf{Clingo Solver}};
    \node[decision, below=0.8cm of clingonode]
      (check) {\textbf{Answer Set $= A^*$?}};
    \node[proc=clingo, minimum width=3.0cm, left=2.2cm of check]
      (feedback) {Generate Feedback $\delta_i$};
    \node[terminal=outcol, minimum width=3.2cm, right=2.2cm of check]
      (out) {ASP Program $\Pi_n$};
    \node[terminal=outcol!60!white, minimum width=3.0cm, below=0.8cm of feedback]
      (maxiter) {Max Iterations Reached};

    \begin{scope}[on background layer]
      \node[draw=black!30, dashed, rounded corners=8pt, fill=loopbg,
            fit=(prompt)(llm)(parse)(clingonode)(check)(feedback),
            inner sep=10pt] (loopbox) {};
    \end{scope}
    \node[grplabel, above=2pt of loopbox.north] {ITERATIVE REFINEMENT LOOP};

    \draw[arr] (facts.east)  -- ++(0.5,0) |- (prompt.west);
    \draw[arr] (model.east)  -- (prompt.west);
    \draw[arr] (format.east) -- ++(0.5,0) |- (prompt.west);
    \draw[arr] (prompt.east) -- node[above, font=\sffamily\tiny]{prompt $p_i$} (llm.west);
    \draw[arr] (llm.south)   -- node[right, font=\sffamily\tiny]{raw output} (parse.north);
    \draw[arr] (parse.south) -- node[right, font=\sffamily\tiny]{$\Pi_i$} (clingonode.north);
    \draw[arr] (clingonode.south) -- node[right, font=\sffamily\tiny]{answer set} (check.north);
    \draw[arr] (check.west)  -- node[above, font=\sffamily\tiny\bfseries]{No}  (feedback.east);
    \draw[darr](feedback.north) -- ++(0,0.55) -| node[pos=0.25, above, font=\sffamily\tiny]{$\delta_i$ fed back} (prompt.north);
    \draw[arr] (check.east)  -- node[above, font=\sffamily\tiny\bfseries]{Yes} (out.west);
    \draw[darr](feedback.south) -- node[right, font=\sffamily\tiny]{max iter} (maxiter.north);

  \end{tikzpicture}%
  }

  \caption{Overview of the LAMP system architecture.}
  \label{fig:lamp_process}
\end{figure}

The LAMP framework operates on a formally defined benchmark structure that constrains the hypothesis space and the target answer set, as illustrated in Figure~\ref{fig:lamp_process}. This section defines that benchmark framework, describes how benchmark instances are generated, and then details the LAMP interaction protocol and evaluation criteria.

\subsection{Benchmark Framework}\label{sec:benchmark}

To keep the benchmarks tractable for both LLM generation and ILP search, the ASP used here is deliberately restricted. Programs consist only of unary \textit{facts} of the form \texttt{descriptor(object)} and \textit{normal rules} of the form \texttt{new\_descriptor(X) :- descriptor\_0(X), [not] descriptor\_1(X), \ldots} which derive new object descriptions from initial instance facts. This subset is expressive enough to require fairly complex rule induction while remaining within a reasonable hypothesis space.

The following is an example representing the model facts that objects \texttt{"car"} and \texttt{"scooter"} are both vehicles, represented with the predicate \texttt{vehicle}, and the scooter is slow moving, represented with the predicate \texttt{slow\_moving}. It also has the rule that if an object is a vehicle and not slow moving it is highway possible, represented with the predicate \texttt{highway\_possible}.

\begin{lstlisting}[language=ASP, caption={Vehicle example ASP program.}]
vehicle(scooter).
vehicle(car).
slow_moving(scooter).
highway_possible(X) :- vehicle(X), not slow_moving(X).
\end{lstlisting}

Running this results in the answer set \texttt{\{ vehicle(scooter), vehicle(car), slow\_moving(scooter), highway\_possible(car) \}}. Given these facts and rules, \texttt{highway\_possible(car)} can be deduced, indicating the car is highway possible and, due to default negation, the scooter is not highway possible.

This restricted ASP subset can now be formalized. A \emph{Problem Profile} specifies how many predicates and terms exist, how many facts are given, and how large the rules may be. Together with the input facts and a target answer set, it defines a concrete benchmark that both LAMP and ILASP can solve.

Previous work on LLM based logic rule induction has characterized complexity by structural rule graph topology such as Chain, RDG (Rooted Directed Graph), DRDG (Disjunctive Rooted Directed Graph), and Mixed categories \cite{gandarela2025inductivelearninglogicaltheories}. Since this work is concerned with correct answer set deduction and rule optimization rather than rule graph structure, complexity is instead quantified by six numerical features. These include number of possible predicates, terms, facts, rules, positive body literals per rule, and negative body literals per rule. All generated programs are also limited to having exactly one answer set.

\begin{definition}[Problem Profile]
\label{def:complexity}
A \emph{Problem Profile} is a tuple
\[
  \mathcal{C} \;=\; \langle\, \mathcal{D},\; \mathcal{T},\; N_F,\; R,\; L^+,\; L^-,\; A_{\min} \,\rangle
\]
where
\begin{itemize}
  \item $\mathcal{D}$ is a finite set of predicate symbols.
  \item $\mathcal{T}$ is a finite set of term constant symbols.
  \item $N_F \in \mathbb{N}$ is the number of \emph{instance facts}.
  \item $R \in \mathbb{N}$ is the maximum number of \emph{rules}.
  \item $L^+ \in \mathbb{N}$ is the maximum number of \emph{positive literals} in each rule body.
  \item $L^- \in \mathbb{N}$ is the maximum number of \emph{negative literals} in each rule body.
  \item $A_{\min} \in \mathbb{N}$ is the \emph{minimum derived atoms}, the minimum number of atoms that must appear in the answer set beyond the given input facts.
\end{itemize}
\end{definition}

From a Problem Profile a concrete input instance can be defined. Each instance pairs a set of ground facts and a target answer set with a bounded schema that defines the hypothesis space both LAMP and ILASP search over.

\begin{definition}[Input Instance]
\label{def:input}
Given a Problem Profile stated as $\mathcal{C} = \langle\, \mathcal{D},\; \mathcal{T},\; N_F,\; R,\; L^+,\; L^-,\; A_{\min} \,\rangle$ (Definition~\ref{def:complexity}), an \emph{instance} of $\mathcal{C}$ is a tuple
\[
  \mathcal{M} \;=\; \langle\, F,\; A^{*},\; \Phi \,\rangle
\]
where
\begin{itemize}
  \item $F$ is the \emph{instance facts}, a set of $N_F$ ground atoms of the form $d(o)$ with descriptor $d \in \mathcal{D}$ and object $o \in \mathcal{T}$ (e.g.\ \texttt{d0(o0)}), so $|F| = N_F$.
  \item $A^{*}$ is the \emph{target answer set}, the desired unique stable model that any correct solution $\Pi$ must produce. It must satisfy $F \subseteq A^{*}$ and $|A^{*} \setminus F| \geq A_{\min}$.
  \item $\Phi$ is the \emph{hypothesis space}, the full set of concrete ASP rules from which $\Pi$ is drawn. Each rule in $\Phi$ has the form
    \[
      d_h(X) \;:\text{-}\; p_1(X),\; \ldots,\; p_{k}(X),\; \texttt{not}\; n_1(X),\; \ldots,\; \texttt{not}\; n_{m}(X).
    \]
    where $d_h,\, p_i,\, n_j \in \mathcal{D}$, $X$ is a single universally quantified variable, $k \leq L^+$ positive body literals, and $m \leq L^-$ negative body literals (bounds from $\mathcal{C}$, Definition~\ref{def:complexity}).
\end{itemize}
A \emph{candidate program} $\Pi$ is any subset $\Pi \subseteq \Phi$ with $|\Pi| \leq R$. Every rule in $\Pi$ is therefore unary, uses only predicates from $\mathcal{D}$, and respects the bounds $L^+$ and $L^-$.
$\Pi$ is a \emph{solution} to $\mathcal{M}$ if and only if
\[
  AS\!\left(F \cup \Pi\right) \;=\; \bigl\{\, A^{*} \,\bigr\}
\]
where $AS(\cdot)$ denotes the set of answer sets of a program. Meaning the unique answer set of $F \cup \Pi$ is exactly $A^{*}$.
\end{definition}

The hypothesis space $\Phi$ corresponds directly to the mode bias of the associated ILASP learning task generated for each benchmark.

\subsection{Dataset Generation}

To instantiate benchmarks across the complexity categories defined above, synthetic data is generated by introducing new possible predicates and terms as needed. To keep the programs sufficiently complex, but manageable by the LLMs, only one stable model is expected and at least one atom must be deduced. The vehicle example above would be represented as shown below.

\begin{lstlisting}[language=ASP, caption={Benchmark ASP program.}]
d0(o0).
d0(o1).
d1(o1).
d2(X) :- d0(X), not d1(X).
\end{lstlisting}

This results in the answer set \texttt{\{\ d0(o0), d0(o1), d1(o1), d2(o0) \}}, corresponding to the vehicle example answer set.

From each benchmark instance $\mathcal{M}$, a corresponding ILASP task file is generated. The predicates that appear in derived atoms $A^{*} \setminus F$ are declared as \texttt{\#modeh} atoms, and all predicates in $\mathcal{D}$ are declared as \texttt{\#modeb} atoms in both positive and negative forms. Since each benchmark has exactly one answer set, the derived predicates are uniquely determined by $A^{*}$, making the \texttt{\#modeh} declarations both necessary and sufficient. No valid solution can require a head predicate not already present in $A^{*} \setminus F$. The target answer set $A^{*}$ is encoded as a single context dependent \texttt{\#pos} example with the instance facts $F$ as context. The inclusion set contains the derived atoms $A^{*} \setminus F$, the exclusion set contains atoms formed by applying each derived predicate to all terms not present in $A^{*}$, and the context contains the instance facts. The corresponding ILASP task for the vehicle example above is shown below.

\begin{lstlisting}[language=ASP, caption={Corresponding ILASP task.}]
#constant(obj, o0).
#constant(obj, o1).

#modeh(d2(var(obj))).

#modeb(1, d0(var(obj))).
#modeb(1, d0(var(obj)), (negative)).
#modeb(1, d1(var(obj))).
#modeb(1, d1(var(obj)), (negative)).
#modeb(1, d2(var(obj))).
#modeb(1, d2(var(obj)), (negative)).

#pos(eg1, {
  d2(o0)
}, {
  d2(o1)
}, {
  d0(o0).
  d0(o1).
  d1(o1).
}).
\end{lstlisting}

This gives ILASP the same hypothesis space over which LAMP operates. For benchmarks with a sufficiently small hypothesis space, a search space file can be generated by running ILASP in enumeration mode.

\subsection{Prompting and Feedback Loop}

The initial prompt instructs the LLM to reconstruct ASP rules that, combined with the given facts, yield the target stable model. It states the rule schema constraints from $\Phi$ (single variable $X$, normal rules with bounded positive and negative body literals, no new facts), tells the LLM to prefer the minimal rule set, and provides the instance facts $F$ and target answer set $A^*$ as concrete inputs. The prompt also contains a short worked example demonstrating the expected output format. If Clingo reports a mismatch, a feedback message is appended to the conversation containing the facts, the expected stable model, and the actual stable model from the candidate rules, asking the LLM to produce corrected rules. This iterative interaction is formalized below.

\begin{definition}[LAMP Interaction Protocol]
\label{def:protocol}
A \emph{LAMP Interaction Sequence} for instance $\mathcal{M} = \langle F, A^{*}, \Phi \rangle$
is a sequence of pairs
\[
  \bigl\langle\,
    (\Pi_0,\delta_0),\; (\Pi_1,\delta_1),\; \ldots,\; (\Pi_n,\delta_n)
  \,\bigr\rangle
\]
where each $\Pi_i$ is a candidate program generated by the LLM given $F$, $A^{*}$, and all prior feedback,
and $\delta_i$ is the \emph{feedback signal} produced by running $F \cup \Pi_i$ through Clingo:
\[
  \delta_i \;=\;
  \begin{cases}
    \texttt{correct}
      & \text{if } AS(F \cup \Pi_i) = \{A^{*}\}, \\[4pt]
    \texttt{unsatisfiable}
      & \text{if } AS(F \cup \Pi_i) = \emptyset, \\[4pt]
    \bigl\langle\, AS(F \cup \Pi_i),\; A^{*} \,\bigr\rangle
      & \text{otherwise.}
  \end{cases}
\]
The sequence terminates when $\delta_n = \texttt{correct}$ or a maximum iteration count is reached.
The final output $\Pi_n$ is accepted as the solution if it satisfies $\mathcal{M}$ (Definition~\ref{def:input}).
\end{definition}

\subsection{Evaluation}

LAMP and ILASP systems are evaluated by correctness of answer set, time to solve and optimality of ASP programs. The ASP programs generated are evaluated after being run via Clingo. The correctness, number of rules, complexity and how close it is to the expected stable model can then be assessed.

The prompt also instructs the LLM to prefer the minimal rule set with fewest rules and literals, directly encouraging concise encodings.

Beyond correctness, the systems are evaluated on whether the LLM has produced a more concise encoding than the original synthetic program, formalized as follows.

\begin{definition}[Program Conciseness]
\label{def:conciseness}
Given a solution $\Pi$ to instance $\mathcal{M}$, its \emph{literal count} is
\[
  L(\Pi) \;=\; \sum_{r\,\in\,\Pi} \bigl(|\mathit{pos}(r)| + |\mathit{neg}(r)|\bigr),
\]
where $\mathit{pos}(r)$ and $\mathit{neg}(r)$ are the positive and negative body literals of rule $r$, and $L(\Pi)$ is the total number of body literals across all rules. A solution $\Pi$ is \emph{more concise} than $\Pi'$ if $L(\Pi) < L(\Pi')$.
\end{definition}

Many other notions of optimality are applicable to ASP programs, including body literal and rule count, stratification, tightness, total grounded rules, and ASP solving time. This work reports both literal count $L(\Pi)$ and unique literal count, the number of distinct body literals across all rules in $\Pi$. While $L(\Pi)$ measures total deductive weight, unique literal count captures predicate vocabulary breadth, where a program that reuses few predicates across many rules is structurally simpler than one with many distinct dependencies, even at the same $L(\Pi)$. Stratification is reported separately.

\section{Experiments}

Experiments were conducted on the synthetic data with varying complexities as specified in the \textit{Dataset Generation} section. A variety of online and local open weight LLMs were selected for testing to get a broad view of capabilities.

\subsection{Experimental Setup}

A total of 120 synthetic ASP programs were created with varying features for testing. The LLMs chosen included \textit{gpt-5-mini}, which was run via OpenAI's API platform, as well as open weight models \textit{gpt-oss:20b} and \textit{qwen3-coder:30b} which were run locally using Ollama. All ASP solving and analysis was done using Clingo. ILASP was run on each benchmark with a timeout of 420 seconds. Benchmarks not solved within this limit were recorded as failures. Each LAMP run was allowed a maximum of 3 iterations, one initial generation followed by up to 2 rounds of feedback verified by Clingo. Both systems receive the same input instance $\mathcal{M}$ (Definition~\ref{def:input}), where the target answer set $A^*$ is provided directly to the LLM in the prompt and encoded as a \texttt{\#pos} example for ILASP. The selected parameters used for complexity classes are listed in Table~\ref{tab:parameters}. All combinations were used except where the negative literal count equals or exceeds the overall literal count, which would leave no positive literals.

\begin{table}[h]
    \centering
    \caption{Parameters associated with Definition~\ref{def:complexity}. Overall Literals is a generation constraint on the sum $L^+ + L^-$ and is not a profile parameter.}
    \label{tab:parameters}
    \begin{tabular}{ll}
    \toprule
    \textbf{Parameter} & \textbf{Variations} \\
    \midrule
    Descriptors & [6, 10] \\
    Objects & [10] \\
    Facts & [6, 9] \\
    Rules & [3, 6, 11] \\
    Overall Literals & [2, 3] \\
    Negative Literals & [0, 1, 2] \\
    Min Derived Atoms & [1, 3] \\
    \bottomrule
    \end{tabular}
\end{table}

\subsection{Results and Discussion}

Overall results are shown in Table~\ref{tab:ilasp_results}. Both \textit{gpt-5-mini} and \textit{gpt-oss:20b} perform very well, deducing the correct ASP rules to get the expected stable model in almost all cases. LAMP using \textit{qwen3-coder:30b} performed significantly worse, reaching only 45.0\% final accuracy (54/120), with 30.8\% correct on the first attempt. Although \textit{qwen3-coder:30b} is tuned for programming, it may not have been tuned for ASP, resulting in poorer performance.

ILASP achieved 97.5\% accuracy (117/120) of which all three failures were timeouts, not reasoning errors, occurring on benchmarks with high negation counts and large numbers of derived atoms. LLMs in LAMP, \textit{gpt-5-mini} and \textit{gpt-oss:20b}, solved 3 and 1 of the benchmarks, respectively, on which ILASP timed out, demonstrating that LLM guided search can succeed in some instances where ILP search cannot.

The iterative feedback loop contributes meaningfully for all models. First attempt accuracy was 94.2\% for \textit{gpt-5-mini} (113/120), 90.0\% for \textit{gpt-oss:20b} (108/120), and 30.8\% for \textit{qwen3-coder:30b} (37/120). Subsequent Clingo verified feedback iterations recovered 7 additional cases for \textit{gpt-5-mini}, 7 for \textit{gpt-oss:20b}, and 17 for \textit{qwen3-coder:30b}. In all cases where the correct stable model was produced, the generated rules never exactly matched those of the original synthetic program, as multiple rule sets can produce the same answer set. Given the sample size of 120 benchmarks, differences of a few cases between systems should be interpreted with caution, as statistical significance may vary.

\begin{table}[h]
    \centering
    \caption{Comparison of all systems including ILASP baseline. First attempt is the accuracy on the initial generation. Final accuracy is after up to 2 feedback iterations. ILASP failures are all timeouts (420\,s limit). LLM failures are unresolved after all iterations.}
    \label{tab:ilasp_results}
    \begin{tabular}{lrrrl}
    \toprule
    System & First attempt & Final & Failures & Failure type \\
    \midrule
    gpt-5-mini       & 113/120 (94.2\%) & 120/120 (100\%)  & 0  & --- \\
    gpt-oss:20b      & 108/120 (90.0\%) & 115/120 (95.8\%) & 5  & Max iterations \\
    qwen3-coder:30b  & 37/120 (30.8\%)  & 54/120 (45.0\%)  & 66 & Max iterations \\
    ILASP            & ---              & 117/120 (97.5\%) & 3  & Timeout only \\
    \bottomrule
    \end{tabular}
\end{table}

Table~\ref{tab:ilasp_timeout} details the three benchmarks on which ILASP timed out (benchmark indices 7, 27, and 67). All three have elevated grounded rules and hypothesis space sizes relative to the dataset average (Table~\ref{tab:timeout_profile}), indicating a larger search space as the cause of timeout. Benchmark 67 has a notably smaller hypothesis space than 7 and 27 yet still timed out, suggesting grounded rules and negation complexity are sufficient to cause timeout independent of hypothesis space size alone. \textit{gpt-5-mini} solved all three, \textit{gpt-oss:20b} solved benchmark 7 but failed on 27 and 67 while \textit{qwen3-coder:30b} failed all three.

\begin{table}[h]
\centering
\caption{Outcomes for the 3 benchmarks where ILASP timed out (420\,s limit). \checkmark = correct stable model produced. $\times$ = failed.}
\label{tab:ilasp_timeout}
\begin{tabular}{lcccc}
\toprule
Benchmark & gpt-5-mini & gpt-oss:20b & qwen3-coder:30b & ILASP \\
\midrule
7  & \checkmark & \checkmark & $\times$ & Timed out \\
27 & \checkmark & $\times$   & $\times$ & Timed out \\
67 & \checkmark & $\times$   & $\times$ & Timed out \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\caption{Structural profile of the 3 ILASP timeout benchmarks vs.\ dataset average.}
\label{tab:timeout_profile}
\begin{tabular}{lrrr}
\toprule
Benchmark & Grounded rules & Hypothesis space & Negative literals \\
\midrule
7           & 27 & 19906 & 11 \\
27          & 20 & 16960 & 11 \\
67          & 27 &  2206 & 11 \\
\midrule
Dataset avg & 13.28 & 3255.22 & 8.80 \\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:outcome_stacked} shows the breakdown of benchmark outcomes per LLM across the three categories of correct on the first attempt (green), correct only after Clingo verified feedback (gold), and failed across all iterations (red).

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.95\textwidth]{diagrams/outcome_stacked.pdf}
    \caption{Stacked bar chart of benchmark outcomes per model showing first attempt success (green), feedback success (gold), and failed (red).}
    \label{fig:outcome_stacked}
\end{figure}

Figure~\ref{fig:timing_boxplot} shows the per benchmark wall clock time distribution for each system as a box plot. \textit{gpt-5-mini} is the fastest LLM with a median of 18.0 and a maximum of 137.0, reflecting the low latency of the API hosted model. \textit{gpt-oss:20b} is slowest with median 50.8 and max 369.6 seconds due to the larger locally run model size. ILASP has a highly bimodal distribution. The median is only 0.66 seconds as most benchmarks are trivially solved by conflict driven search, but three benchmarks hit the 420.0 second ceiling, pulling the maximum to that value. \textit{qwen3-coder:30b} shows a near uniform distribution with a median of 40.3 and a maximum near the 420.0 timeout at 417.8 seconds, consistent with the model spending maximum iteration budget on hard cases it ultimately fails to solve. Table~\ref{tab:timing} gives the aggregate statistics.

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.95\textwidth]{diagrams/timing_boxplot.pdf}
    \caption{Per benchmark wall clock time distribution per system (seconds). Boxes show the interquartile range. Whiskers extend to the full range. ILASP failures are capped at the 420\,s timeout.}
    \label{fig:timing_boxplot}
\end{figure}

\begin{table}[h]
    \centering
    \caption{Wall clock time per system (seconds). LLM times accumulate all feedback iterations. ILASP timeouts are recorded as 420\,s.}
    \label{tab:timing}
    \begin{tabular}{lrrrr}
    \toprule
    Model & Total (s) & Mean (s) & Median (s) & Max (s) \\
    \midrule
    gpt-5-mini       & 2837.00 & 23.64 & 18.00 & 137.00 \\
    gpt-oss:20b      & 8579.97 & 71.50 & 50.78 & 369.64 \\
    qwen3-coder:30b  & 5319.65 & 44.33 & 40.32 & 417.77 \\
    ILASP            & 1917.36 & 15.98 &  0.66 & 420.00 \\
    \bottomrule
    \end{tabular}
\end{table}

Table~\ref{tab:lit_by_correctness} gives the mean output literal count $L(\Pi)$ (Definition~\ref{def:conciseness}) per system at original literal counts of 22 and 33, split by correctness. Correct solutions show a large reduction relative to the originals across all systems, averaging 3 to 5 literals against originals of 22 or 33. This is consistent with the synthetic benchmark structure, where generated programs may include rules not strictly necessary to produce the target answer set.

\begin{table}[h]
\centering
\caption{Mean output literal count $L(\Pi)$ per system for benchmarks with 22 and 33 original body literals, split by correctness. \texttt{--} indicates no data.}
\label{tab:lit_by_correctness}
\begin{tabular}{llrrrr}
\toprule
Orig. literals & & gpt-5-mini & gpt-oss:20b & qwen3-coder:30b & ILASP \\
\midrule
\multirow{2}{*}{22} & Correct   & 4.81 & 4.91 & 4.40 & 3.67 \\
                    & Incorrect & --   & 7.00 & 4.70 & --   \\
\midrule
\multirow{2}{*}{33} & Correct   & 3.54 & 3.96 & 4.26 & 2.71 \\
                    & Incorrect & --   & 24.00 & 3.03 & --   \\
\bottomrule
\end{tabular}
\end{table}

Figures \ref{fig:gpt_5_literals}, \ref{fig:gpt_oss_literals}, \ref{fig:qwen3_literals}, and \ref{fig:ilasp_literals} compare the unique literal count of the original ASP programs against that of ILASP and the LLM generated rules for each LLM tested using LAMP. Dot size indicates the number of examples. The left plot shows correct answer sets and the right shows first attempt failures with recovered cases in gold and failed cases in red.

Figure \ref{fig:gpt_5_literals} shows \textit{gpt-5-mini} is generally able to greatly reduce the number of literals needed, in some cases reducing the number of literals by 6, but in some cases increasing them by 2. As shown in Figure~\ref{fig:gpt_oss_literals}, \textit{gpt-oss:20b} had similar results generally needing even fewer literals, while \textit{qwen3-coder:30b} (Figure~\ref{fig:qwen3_literals}) had mixed results. ILASP (Figure~\ref{fig:ilasp_literals}) is similarly able to reduce literal counts, though it fails to improve conciseness on the 3 benchmarks where it timed out.

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.495\textwidth]{diagrams/Correct_gpt-5-mini.pdf}
    \includegraphics[width=0.495\textwidth]{diagrams/Incorrect_gpt-5-mini.pdf}
    \caption{Unique literal counts for \textit{gpt-5-mini} (correct on left, recovered in gold and failed in red on right).}
    \label{fig:gpt_5_literals}
\end{figure}

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.495\textwidth]{diagrams/Correct_gpt-oss:20b.pdf}
    \includegraphics[width=0.495\textwidth]{diagrams/Incorrect_gpt-oss:20b.pdf}
    \caption{Unique literal counts for \textit{gpt-oss:20b} (correct on left, recovered in gold and failed in red on right).}
    \label{fig:gpt_oss_literals}
\end{figure}

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.495\textwidth]{diagrams/Correct_qwen3-coder:30b.pdf}
    \includegraphics[width=0.495\textwidth]{diagrams/Incorrect_qwen3-coder:30b.pdf}
    \caption{Unique literal counts for \textit{qwen3-coder:30b} (correct on left, recovered in gold and failed in red on right).}
    \label{fig:qwen3_literals}
\end{figure}

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.495\textwidth]{diagrams/Correct_ilasp.pdf}
    \includegraphics[width=0.495\textwidth]{diagrams/Incorrect_ilasp.pdf}
    \caption{ILASP unique literal counts (correct on left, timed out in red on right).}
    \label{fig:ilasp_literals}
\end{figure}

\subsection{Stratification Analysis}

An ASP program is \emph{stratified} if no predicate depends negatively on itself (directly or transitively). Stratification is a structural property that simplifies semantics and is often correlated with lower solver complexity. Since the LAMP benchmarks include programs with varying negation depths, it is informative to assess whether LLM generated programs preserve this property.

\begin{table}[h]
    \centering
    \caption{Stratification of solved programs per system. Original is the 120 benchmark programs.}
    \label{tab:llm_stratification}
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{lr}
    \toprule
    System & Stratified / Solved \\
    \midrule
    Original        & 49 / 120 \\
    gpt-5-mini      & 119 / 120 \\
    gpt-oss:20b     & 114 / 115 \\
    qwen3-coder:30b & 54 / 54 \\
    ILASP           & 117 / 117 \\
    \bottomrule
    \end{tabular}
\end{table}

Table~\ref{tab:llm_stratification} shows that both ILASP and the LLM based systems produce programs that are substantially more stratified than the original benchmark programs, of which only 49 of 120 were stratified. ILASP, being a formal inductive learner, produces stratified programs in 117 of its 117 correct solutions. Among the LLMs, \textit{gpt-5-mini} achieves the highest degree of stratification, with 119 of its 120 correct programs being stratified, closely followed by \textit{gpt-oss:20b} at 114 of 115. Even \textit{qwen3-coder:30b}, which solved far fewer benchmarks, produced stratified programs in all 54 of its correct solutions. The degree of stratification indicates both learned and generated programs tend toward syntactic simplicity that avoids the cyclic negation present in the synthetic benchmarks.

\subsection{Feature Importance}

Based on whether the LLM is able to get the correct stable model or not it is possible to deduce which ASP benchmark features are most difficult for LLMs to deduce thereby indicating higher complexity. To do this, syntactic and structural features of the input ASP program, such as number of predicates, facts, rules, and literal counts, are used to predict the likelihood of getting the correct stable model using the tree based machine learning algorithm XGBoost \cite{chen2016xgboostscalabletreeboosting}, trained on the \textit{qwen3-coder:30b} results. The other two LLMs had too few failures for a meaningful classifier.

From the XGBoost model the importance value of each feature can be calculated. There are multiple ways to get importance, such as gain, the average improvement in the loss function from splits using that feature, or cover, the average number of samples affected by splits using that feature. For this analysis gain is used.

The results can be seen in Figure~\ref{fig:importance}. The most important feature is \textit{Solution atoms}, the total number of atoms in the answer set, serving as a proxy for overall problem complexity. The next most important is \textit{Derived atoms}, the number of atoms that must be deduced beyond the input facts, directly reflecting the deductive burden placed on the LLM. \textit{Negative literals} also ranks highly, consistent with default negation being a known challenge for LLMs. As the XGBoost model is trained on a small sample, these importances should be treated as indicative rather than definitive.

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.9\textwidth]{diagrams/xgb_importance.pdf}
    \caption{XGBoost feature importance of ASP features predicting LLM success.}
    \label{fig:importance}
\end{figure}

\section{Related Work}

\subsection{Inductive ASP Learning and LAS}

Recent work has explored the integration of LLMs and ASP with several approaches studying how LLMs can learn from or reason over ASP directly. ILASP \cite{law2020ilaspinductivelearninganswer}, the leading ILP system for Learning from Answer Sets was combined with LLMs by Borroto et al. \cite{borroto2025questionansweringllmslearning} which investigated question answering and inductive learning from answer sets, introducing the LLM2LAS system, which uses LLMs to construct ILASP learning tasks from natural language stories and then runs ILASP to induce the required commonsense rules.

A key distinction from LAMP is that LLM2LAS uses LLMs to \emph{build the ILP task} and then defers to ILASP for search, whereas LAMP uses LLMs to \emph{directly generate} the hypothesis, bypassing exhaustive search. Declarative knowledge distillation techniques have been proposed to transfer structured reasoning from LLMs into symbolic representations by Eiter et al. (2024) \cite{eiter2024declarativeknowledgedistillationlarge}.

\subsection{Expressivity Graded Evaluation}

Gandarela et al.~\cite{gandarela2025inductivelearninglogicaltheories} demonstrated that LLMs can perform inductive logic programming, learning logic rules from background knowledge and positive and negative examples when combined with feedback from a formal inference engine such as Prolog. The authors introduced the RuDaS benchmark framework and a systematic methodology for evaluating LLM based theory induction graded by rule expressivity as Chain, RDG, DRDG, and Mixed categories. A key finding from that work is that LLMs are limited by their capacity to track long predicate dependency chains, directly relevant to future LAMP experiments and improvements.

Beyond ASP specific research, logic programming has been used as a tool to evaluate and scaffold LLM reasoning. Hybrid GOFAI LLM systems show how expert systems can be rebuilt using LLM generated logic rules \cite{garridomerchán2026gofaimeetsgenerativeai}, while Xu et al. (2025) \cite{xu2025socratessmartypantstestinglogic} shows programming based test evaluators systematically assess LLM reasoning reliability. These works support the use of formal solvers, as in LAMP, to ground and validate LLM outputs.

\subsection{LLM Benchmarking on ASP}

ASPBench \cite{szeider2026aspbenchnaturallanguagelogic} provides benchmarks specifically for grading LLM ASP induction. Ren et al.~\cite{ren2025llmssolveaspproblems} benchmark 14 LLMs across ASP entailment, verification, and answer set computation tasks, finding that answer set computation remains a significant challenge for LLMs without scaffolding, suggesting that structured prompting and iterative solver feedback, as in LAMP, are likely necessary for reliable ASP induction. Schrader et al.~\cite{schrader2025solverintheloopframeworkimprovingllms} adopt a related solver-in-the-loop strategy, but use solver feedback to curate training data for supervised fine tuning on natural language to ASP translation for logic puzzles, whereas LAMP uses solver feedback at inference time to guide rule induction from facts and a target answer set. LLM-ARC \cite{kalyanpur2024llmarcenhancingllmsautomated} adopts an actor critic framing in which an LLM generates logic and an automated reasoning critic evaluates it, and Kaur et al.~\cite{kaur2025empiricalstudyconformalprediction} use ASP as a scaffold to improve LLM reasoning robustness under conformal prediction. Both pair LLMs with formal solvers in a pattern similar to LAMP, applied to general reasoning rather than rule induction.

\subsection{Planning and NeuroSymbolic Pipelines}

Planning provides another closely related line of research. Logical chain of thought tuning for symbolic planning by Verma et al. (2025) \cite{verma2025teachingllmsplanlogical} and LLM symbolic planning pipelines without expert input  by Huang et al. (2024) \cite{huang2024planningdarkllmsymbolicplanning} highlight the benefits of keeping solvers such as ASP planners in the loop. Established ASP planning benchmarks, including Plasp developed by Dimopoulos et al. \cite{dimopoulos2018plasp3effectiveasp}, offer realistic domains that could extend LAMP beyond product configuration. Neuro symbolic refinement frameworks such as PEIRCE further demonstrate how LLMs and formal reasoning systems can iteratively improve each other \cite{quan2025peirceunifyingmaterialformal}. Hybrid LLM and ASP systems have also been applied to natural language understanding \cite{rajasekharan2023reliablenaturallanguageunderstanding}, commonsense reasoning \cite{zeng2024reliablecommonsensereasoningsocialbot}, and collaborative conversational agents \cite{zeng2025reliablecollaborativeconversationalagent}.

\section{Future Work}

Several directions for future work follow naturally. First, iterative prompting could be improved by more explicit chain of thought reasoning, potentially evaluating many candidate rules across multiple instance fact sets per iteration, with Clingo acting as a tool within an LLM based agent loop. Another approach would be from the specific perspective of solving complexity, focusing on solution space features such as vector cover size, tree depth, feedback vertex set, clique width, tree width, among others \cite{hecher2024extendedversionofstructural}. Prompt optimization frameworks such as DSPy \cite{khattab2023dspycompilingdeclarativelanguage} could be used to automatically refine prompts and feedback strategies. Even without using LLMs fine tuned to ASP \cite{coppolillo2024llaspfinetuninglargelanguage}, the models are always improving, so simply updating the models may result in large improvements.

Second, the synthetic datasets could be extended to richer ASP constructs, including higher arity predicates, choice rules, disjunction, and head disjunction, as well as more complex dependency structures such as those proposed by Gandarela et al. (2025) regarding logic program expressivity \cite{gandarela2025inductivelearninglogicaltheories}.

Third, evaluation could move beyond synthetic configuration tasks to existing ASP datasets, including ASPBench \cite{szeider2026aspbenchnaturallanguagelogic}, OOASP models \cite{falkner2015ooaspconnectingobjectorientedlogic}, real world ASP encodings, planning benchmarks such as Plasp and PDDL to ASP translations \cite{dimopoulos2018plasp3effectiveasp}, and domains like rail scheduling.

Fourth, deeper natural language interfaces to ASP could be explored, covering bidirectional translation between text and ground facts, rules, stable models, and multiple natural languages.

Finally, optimization remains a promising avenue, including other ways of transforming ASP into more efficient ASP, compiling ASP fragments to lower level code, such as C, and tighter integration with solver level optimizations, enabling LLMs to reason not only about correctness but also about grounding and solving performance.

\section{Conclusion}

This work proposed a structured LLM to ASP Modeling Protocol (LAMP) designed to support the deduction and optimization of ASP rules from instance facts and expected stable models. The results show this is effective for guiding LLMs toward correct and often optimized ASP encodings. By constraining the modeling task and providing iterative feedback via an ASP solver, this system reduces the modeling burden associated with ASP while preserving correctness.

The tested models exhibited clear performance differences, with \textit{gpt-5-mini} achieving 100\% final accuracy (94.2\% on first attempt) and \textit{gpt-oss:20b} achieving 95.8\% (90\%), while \textit{qwen3-coder:30b} reached only 45\% (30.8\% first attempt) despite its programming focus. These results highlight that strong general code generation ability does not necessarily translate to proficiency in logic programming, underscoring the value of ASP specific evaluation frameworks.

The ILASP baseline achieved 97.5\%, with the three failures being search space timeouts rather than reasoning errors. This shows LLMs do not follow the same classical ILP search. They navigate the hypothesis space differently, using token-level generation rather than conflict driven search, and can succeed where conflict driven search may not. The iterative feedback loop further contributed recovery of many cases for the three LLMs, confirming its value as a component of the LAMP pipeline. ILASP was faster on average but timed out on three benchmarks that LAMP solved, while both systems substantially reduced literal counts relative to the synthetic originals and produced more stratified programs than the benchmarks themselves. Feature analysis on \textit{qwen3-coder:30b} identified solution atom count, derived atom count, and negative literal count as the strongest predictors of LLM failure.

Beyond assisting users in ASP modeling, this protocol provides a system for probing LLM reasoning and logic programming capabilities. Future work includes extending to richer ASP constructs such as disjunction and choice rules, incorporating solver level performance metrics, and exploring fine tuned or neuro symbolic hybrids to further improve robustness and scalability.

\bibliography{custom}
\bibliographystyle{plain}

\end{document}
